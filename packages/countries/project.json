{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "release": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "npx semantic-release-plus --extends=./packages/countries/release.config.js",
        "parallel": false
      }
    }
  }
}
